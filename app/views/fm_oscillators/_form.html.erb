<%= form_with(model: fm_oscillator) do |form| %>
  <% if fm_oscillator.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(fm_oscillator.errors.count, "error") %> prohibited this fm_oscillator from being saved:</h2>

      <ul>
        <% fm_oscillator.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.submit %>
  </div>
<% end %>
